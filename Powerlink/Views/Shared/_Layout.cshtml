<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Scripts.Render("~/bundles/jquery")
    @Styles.Render("~/Content/css")
    @Styles.Render("~/Content/css/Datatables")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body class="body">
    <!---    <div class="navbar navbar-inverse navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    @Html.ActionLink("Application name", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
                </div>
                <div class="navbar-collapse collapse">
                    <ul class="nav navbar-nav">
                        <li>@Html.ActionLink("Home", "Index", "Home")</li>
                        <li>@Html.ActionLink("About", "About", "Home")</li>
                        <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
                    </ul>
                    @Html.Partial("_LoginPartial")
                </div>
            </div>
        </div> -->

    @if (Session["username"] != null)
    {
        <div class="signout-info">
            Welcome, @Session["adminfn"] @Session["adminln"]
            | @Html.ActionLink("Sign Out", "Logout", "Admin") |
        </div>
    }
    <div class="container body-content wrapper">
  
            @RenderBody()
            <hr />
    </div>
    <div class="footer">
        <p>&copy; @DateTime.Now.Year - Powerlink</p>
    </div>

    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/HomeDesign")
    @RenderSection("scripts", required: false)
</body>
</html>
